# artflow
This is a Web App created using Google Web Studio. The project files (HTML, CSS, JS) were exported from Google Web Studio, then uploaded to GitHub. This app does not require a build process, as all files are already ready to run in the browser. Netlify will directly publish the static files.
